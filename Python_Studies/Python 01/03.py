############################# if else elif
#age = int(input("How old are you?: "))

#if age == 100:
    #print("You are a century old!")
#elif age >= 18:
    #print("You are an adult!")
#elif age < 0:
    #print("You haven't been born yet!")
#else:
    #print("You are a child!")

############################# operadores lógicos (and,or,not) = usado para checar se dois ou mais estrutura de condição é True // not inverte, ex : sim é true, not faz virar false.

#temp = int(input("What is the temperature outside?: "))

#if not(temp >= 0 and temp <= 30):
    #print("The temperature is bad today!")
    #print("Stay inside!")
    
#elif not(temp < 0 or temp > 30):
    #print("The temperature is good today!")
    #print("Go outside!")

############################# while loop: condição que irá executar um block of code, se for True, irá ficar pra sempre.

#name = ""

#while len(name) == 0:
    #name = input("Enter your name: ")
## pode se escrever também desta forma:
#name = None
#while not name:
    #name = input("Enter your name: ")

#print("Hello {}" .format(name))
